# `姓名`

**`手机号`** · `邮箱` · `性别/城市/求职方向`

![个人照片](assets/profile-photo.jpg)

## 教育经历

**`学校名称`**  `学历`，`起止时间`
`专业名称`；专业成绩：`成绩/排名`；主修课程：`《课程 1》`、`《课程 2》`、`《课程 3》`

**`学校名称`**  `学历`，`起止时间`
`专业名称`；`与目标岗位相关的课程、微专业、科研或校园项目`；曾获荣誉：`荣誉/奖项/证书`

---
## 实习经历

**`公司/组织名称`**  `岗位名称`  `起止时间`

1. **`项目/职责亮点 1`**  
   针对`业务痛点/用户问题`，通过`调研/数据分析/竞品分析/访谈`发现`关键问题`，提出`改进方向/优化方案`，推动`具体方案/功能/策略`落地，达成`量化结果/业务影响`。

2. **`项目/职责亮点 2`**  
   为解决`核心问题`，深入分析`用户行为/业务链路/系统流程`，精细化`规则/策略/产品机制`，在保障`用户体验/业务目标`的同时提升`指标`，例如`转化率/准确率/效率/跳出率`改善`X%/Xpp`。

3. **`项目/职责亮点 3`**  
   针对`新场景/复杂链路/长尾需求`，重构`流程/链路/交互/策略`，将`高价值需求`精准分发至`目标场景`，减少`无效操作/人工成本/错误率`，拓展`业务/技术应用`边界。

**`公司/组织名称`**  `岗位名称`  `起止时间`

> **项目简述**：为解决`业务场景/用户群体`的`核心痛点`，作为`角色`，深度参与`方案规划/落地推进/效果评估`。

1. **`方案定位与架构设计`**  
   针对`传统方案痛点`与`现有流程不足`，通过`用户访谈/业务调研/跨部门评审`，确定`技术框架/方案定位/解决路径`。

2. **`核心能力构建与优化`**  
   针对`非结构化信息/召回率/准确率/效率`问题，提出基于`工具/框架/方法`的`流程/流水线/系统方案`，协调`团队/资源`落地，实现`关键指标`提升`X%`。

3. **`策略创新与效果验证`**  
   针对提升`专业术语查询/任务分发/问题定位`的准确率，提出`混合检索/规则权重/评估体系/策略优化`方案，经`案例验证/数据复盘/A-B 测试`后使`指标 1`提升`X%`，`指标 2`下降`X%`。

4. **`运营体系/知识体系建设`**  
   搭建可持续的`知识运营/流程运营/数据治理`体系，设计`自动分类 + 预处理 + 人工审核`等机制，累计沉淀`数量`条高质量`知识点/案例/数据`，支持`业务部门/用户群体`工作。

5. **`推广运营与用户触达`**  
   负责上线后的`推广运营/用户培训/反馈收集`，通过`培训/内部博客/用户社群/运营活动`等方式，获得`评分/满意度`，实现`访问量/使用率/采纳率`提升`X%`。

---
## 相关技能

1. **工作技能**：具备`需求调研/需求拆解/用户反馈分析/上线运营/跨部门协同/任务池维护/行动项追踪`等能力，能够支撑`目标岗位核心工作场景`。
2. **数据技能**：掌握 `Python`（`Pandas` / `NumPy` / `scikit-learn` 等）、`SQL`、`Excel`（`VLOOKUP` / `数据透视表`），能够完成数据提取、分析、指标复盘和项目材料整理。
3. **AI技能**：熟悉 `RAG/LLM/LangChain/Dify/AI Agent/混合检索/多模态`等 AI 相关工具、方法或项目流程，能够支持`目标岗位相关工作流/智能体应用/方案迭代`。
4. **语言能力**：`雅思/托福/四六级/其他成绩`，具备`听说读写能力描述`，可支持英文会议、邮件沟通、英文资料阅读和双语项目材料整理。

<!--
模板使用说明（生成正式简历时请遵守 SKILL.md）：

1. 章节顺序：姓名与联系方式 → 教育经历 → 实习经历 → 相关技能。
2. 教育经历：每校两行。首行 `**学校**  学历，时间`；次行仅写教育事实（专业、成绩、课程、荣誉等），不写软技能或岗位匹配话术。
3. 实习经历：表头一行 `**公司**  岗位名称  时间`；项目名、JD 关键词写在编号条目中，不写进表头。导出脚本将教育/实习表头渲染为三列对齐，勿用表格表头或全角空格手工对齐。
4. 证件照：`![个人照片](相对路径)`，仅一张，文件须在工作区存在。
5. 事实与指标从 `简历数据库.md` 或原始简历抽取；缺数据写 `[待补充：指标]`，禁止编造。
6. `---` 紧贴在 `## 实习经历`、`## 相关技能` 上一行，中间不留空行。
7. 每段实习/工作最多 5 条编号；逻辑顺序：背景/问题 → 方案/协同 → 落地/验证 → 结果/复盘。
8. 多项目实习：用 `**项目一：名称**` + 下一行简介 + 顶格 `1）**任务：**…`（每项目内从 1 编号）；简介 Markdown 不加粗、导出为灰色斜体且 **1 句点到为止**；量化结果与职责标题 `**加粗**`；经历正文避免中英混杂（见 reference.md）。章节标题导出为灰色底栏。
9. `相关技能` 固定 4 行单行有序列表：`工作技能` / `数据技能` / `AI技能` / `语言能力`；`AI技能` 写泛化能力，不写具体项目名称。
-->
